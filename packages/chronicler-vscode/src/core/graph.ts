// Graph helpers — re-exports from workspace for convenience.
// If graph logic grows beyond what workspace.ts handles, extract it here.

export type { GraphData, GraphNode, GraphEdge } from './types';
